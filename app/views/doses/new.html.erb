<%= Cocktail.find(params[:cocktail_id]).name %>
<%= simple_form_for([Cocktail.find(params[:cocktail_id]), @dose]) do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit %>
<% end %>
<%= link_to "Back to Home", cocktails_path %>
